// Line comment which go to the end of the line
// In general line comments are the recommended comment style

fn main() {
    /*
     * Block comment which go to the closing delimiter
     * Block comments are extremely useful for temporarily
     * disabling chunks of code
     */

     let x = 3 + /* 9 + */ 7;
     println!("x = {}", x);
}
