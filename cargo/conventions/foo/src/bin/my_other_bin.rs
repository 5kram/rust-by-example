// To tell cargo to compile or run this binary as opposed to the default 
// or other binaries, we just pass cargo the --bin my_other_bin flag
// $ cargo run --bin my_other_bin
fn main() {
    println!("Hello, world! from bin");
}
